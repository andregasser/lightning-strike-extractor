# Legacy research material

`prototypes/` contains the original exploratory scripts that preceded the
packaged command-line application. They are retained as implementation history
and may rely on hard-coded filenames or historical artifact paths.

New features, fixes, tests, and supported workflows belong in
`src/lse/` or `tests/`. Do not add production behavior to these
prototypes.
